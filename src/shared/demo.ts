// The demo account seeded by `db:reset` (base seed). Shown on the login page so
// students can sign in immediately. Kept in sync with backend/seeds/e2e-fixtures.ts.
export const DEMO_CREDENTIALS = {
  email: "e2e@test.com",
  password: "123456",
} as const;
